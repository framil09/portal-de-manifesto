{
  "name": "manifestacao-municipios",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "jspdf": "^4.2.1",
    "qrcode": "^1.5.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "xlsx": "^0.18.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "start:api": "npm --prefix backend start",
    "dev:api": "npm --prefix backend run dev",
    "dev:all": "npm run dev:api & npm start",
    "build": "react-scripts build",
    "test": "react-scripts test --watchAll=false"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2"
  }
}
